import 'package:flutter/material.dart';

abstract class AppStyles {
  static Decoration customBoxDecoration = BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.all(
      const Radius.circular(13.0),
    ), // BorderRadius
  );
}
