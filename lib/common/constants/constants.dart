import 'package:flutter/material.dart';

const Size designSize = Size(375, 812);

const int kPerPage = 20;
Decoration decoration=BoxDecoration(
  color: Colors.white,
  borderRadius: BorderRadius.all(
    const Radius.circular(13.0),
  ),// BorderRadius

);